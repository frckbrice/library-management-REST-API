/**
 * CORS Allowed Origins
 *
 * List of origins permitted by the CORS middleware. Use specific URLs in production;
 * avoid "*" when credentials are used.
 */
const allowOrigins = [
    "http://localhost:3000",
    "*"
];

export default allowOrigins;
